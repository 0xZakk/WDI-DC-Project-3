<!DOCTYPE html>
<html>
<head>
  <title>VinylApp</title>
  <%= stylesheet_link_tag :all %>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

    <nav class="navbar navbar-default">
      <div class="container-fluid">

        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
            <li><a class="navbar-brand" href="/" class="active">Vinyl.me</a></li>
        <!-- if current_user = nil -->
            <li><a href="/about">About <span class="sr-only">(current)</span> </a></li>
            <li><%= link_to "Playlists", playlists_path %></li>
        <!-- if current_user != nil, Collection and Wishlist -->
          </ul>

          <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
              <li>
                <%= link_to(current_user.email, {controller: "home", action: "about"}) %>
              </li>
              <li>
                <%= link_to_if(current_user, "Logout", destroy_user_session_path, :method => :delete) %>
              </li>
            <% else %>
              <li>
                <%= link_to("Login", new_user_session_path) %>
              </li>
            <% end %>
          </ul>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>

  <div class="container-fluid">
    <%= yield %>
  </div>

</body>
</html>
